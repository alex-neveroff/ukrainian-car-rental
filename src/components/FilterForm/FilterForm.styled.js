const { default: styled } = require('@emotion/styled');

const FormStyled = styled.form`
  width: 100%;
  display: flex;
  flex-wrap: wrap;
  gap: 18px;
  justify-content: center;
`;

export { FormStyled };
