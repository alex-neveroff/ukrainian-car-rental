const { default: styled } = require('@emotion/styled');

const FormStyled = styled.form`
  width: 100%;
  display: flex;
  gap: 18px;
  justify-content: center;
  margin-bottom: 50px;
`;

export { FormStyled };
