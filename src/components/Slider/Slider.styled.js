import styled from '@emotion/styled';

const SliderStyled = styled.div`
  display: flex;
  justify-content: center;
  flex: 0 0 auto;
`;

const Slide = styled.img`
  width: 100%;
  border-radius: 14px;
`;

export { SliderStyled, Slide };
